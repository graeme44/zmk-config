manifest:
  remotes:
    - name: zmkfirmware
      url-base: https://github.com/zmkfirmware
  projects:
    # Pinned to the last revisions known to run correctly on this keyboard
    # (the 2025-11-19 build). ZMK main moved to Zephyr 4.1 + LVGL 9 on
    # 2025-12-09; on that stack the split peripheral freezes a few seconds
    # after boot and the halves never finish pairing. Revisit once
    # https://github.com/mctechnology17/zmk-nice-oled/pull/37 has merged and
    # settled, then move both back to main together.
    - name: zmk
      remote: zmkfirmware
      revision: e34793e8c7193bc868b95ec38d78f67956f759cb
      import: app/west.yml
  self:
    path: config
