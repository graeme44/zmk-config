on: [push, pull_request, workflow_dispatch]

jobs:
  build:
    # Pinned to the same ZMK revision as config/west.yml. The workflow on main
    # assumes HWMv2 board naming and calls `west boards` with a format string
    # that the pinned Zephyr does not understand, so the two must move together.
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@e34793e8c7193bc868b95ec38d78f67956f759cb
