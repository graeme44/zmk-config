# This file generates the GitHub Actions matrix
# For simple board + shield combinations, add them
# to the top level board and shield arrays, for more
# control, add individual board + shield combinations to
# the `include` property, e.g:
#
# board: [ "nice_nano_v2" ]
# shield: [ "corne_left", "corne_right" ]
# include:
#   - board: bdn9_rev2
#   - board: nice_nano_v2
#     shield: reviung41
#
---
# DECISIVE TEST: built-in status screen (no nice_oled) + SDA remapped to P0.22,
# plus USB logging so the SSD1306 init result can be read over serial.
include:
  - board: nice_nano_v2
    shield: sofle_left
    snippet: zmk-usb-logging
    artifact-name: left-builtin-p022
